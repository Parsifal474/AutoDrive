<Window x:Class="AutoDrive.Views.PaymentEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Платёж" Height="450" Width="450" WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Данные платежа" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>

        <ComboBox Grid.Row="1" ItemsSource="{Binding Students}" DisplayMemberPath="LastName" 
                  SelectedValuePath="Id" SelectedValue="{Binding StudentId, UpdateSourceTrigger=PropertyChanged}"
                  Margin="0,5" Tag="Курсант *"/>

        <TextBox Grid.Row="2" Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Tag="Сумма *"/>
        <DatePicker Grid.Row="3" SelectedDate="{Binding PaymentDate}" Margin="0,5" Tag="Дата платежа"/>
        <TextBox Grid.Row="4" Text="{Binding Description}" Margin="0,5" Tag="Описание"/>
        <ComboBox Grid.Row="5" ItemsSource="{Binding PaymentTypes}" SelectedItem="{Binding PaymentType}" Margin="0,5" Tag="Тип оплаты"/>
        <CheckBox Grid.Row="6" Content="Часть рассрочки" IsChecked="{Binding IsInstallment}" Margin="0,10"/>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Отмена" Click="CancelButton_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="Сохранить" Click="SaveButton_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>