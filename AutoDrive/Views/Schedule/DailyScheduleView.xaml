<Window x:Class="AutoDrive.Views.Schedule.DailyScheduleView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Расписание на день" Height="500" Width="800"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding DateTitle}" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <WrapPanel Grid.Row="1" Margin="0,0,0,10">
            <Button Content="Добавить занятие" Command="{Binding AddCommand}" Margin="0,0,5,0" Padding="10,5"/>
            <Button Content="Изменить" Command="{Binding EditCommand}" Margin="0,0,5,0" Padding="10,5"/>
            <Button Content="Удалить" Command="{Binding DeleteCommand}" Padding="10,5"/>
        </WrapPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Lessons}" SelectedItem="{Binding SelectedLesson}"
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Начало" Binding="{Binding Lesson.StartTime, StringFormat={}{0:HH:mm}}" Width="80"/>
                <DataGridTextColumn Header="Конец" Binding="{Binding Lesson.EndTime, StringFormat={}{0:HH:mm}}" Width="80"/>
                <DataGridTextColumn Header="Ученик" Binding="{Binding StudentName}" Width="150"/>
                <DataGridTextColumn Header="Инструктор" Binding="{Binding InstructorName}" Width="150"/>
                <DataGridTextColumn Header="Автомобиль" Binding="{Binding VehicleTitle}" Width="200"/>
                <DataGridTextColumn Header="Примечание" Binding="{Binding Lesson.Note}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Закрыть" Command="{Binding CloseCommand}" Width="80" Padding="5"/>
        </StackPanel>
    </Grid>
</Window>