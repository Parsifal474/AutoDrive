<UserControl x:Class="AutoDrive.Views.Schedule.MonthScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:AutoDrive.Converters"
             mc:Ignorable="d">
    <UserControl.Resources>
        <local:BoolToBrushConverter x:Key="BoolToBrushConverter"/>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <Button Content="◀" Command="{Binding PreviousMonthCommand}" Width="40" Margin="5"/>
            <TextBlock Text="{Binding MonthYear}" FontSize="20" FontWeight="Bold" Width="200" TextAlignment="Center"/>
            <Button Content="▶" Command="{Binding NextMonthCommand}" Width="40" Margin="5"/>
            <Button Content="Сегодня" Command="{Binding TodayCommand}" Margin="10,0,0,0" Padding="10,5"/>
        </StackPanel>

        <!-- Сетка дней недели -->
        <Grid Grid.Row="1" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Пн" FontWeight="Bold" TextAlignment="Center"/>
            <TextBlock Grid.Column="1" Text="Вт" FontWeight="Bold" TextAlignment="Center"/>
            <TextBlock Grid.Column="2" Text="Ср" FontWeight="Bold" TextAlignment="Center"/>
            <TextBlock Grid.Column="3" Text="Чт" FontWeight="Bold" TextAlignment="Center"/>
            <TextBlock Grid.Column="4" Text="Пт" FontWeight="Bold" TextAlignment="Center"/>
            <TextBlock Grid.Column="5" Text="Сб" FontWeight="Bold" TextAlignment="Center"/>
            <TextBlock Grid.Column="6" Text="Вс" FontWeight="Bold" TextAlignment="Center"/>
        </Grid>

        <!-- Календарь (ItemsControl с UniformGrid) -->
        <ItemsControl Grid.Row="2" ItemsSource="{Binding Days}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="6" Columns="7"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="LightGray" BorderThickness="0.5" 
                            Background="{Binding IsCurrentMonth, Converter={StaticResource BoolToBrushConverter}}">
                        <Button Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}, Path=DataContext.OpenDayCommand}"
                                CommandParameter="{Binding Date}"
                                Background="Transparent" BorderThickness="0">
                            <StackPanel>
                                <TextBlock Text="{Binding DayNumber}" FontSize="16" Margin="5" HorizontalAlignment="Left"/>
                                <Ellipse Width="8" Height="8" Fill="Red" Margin="5" HorizontalAlignment="Left" 
                                         Visibility="{Binding HasLessons, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            </StackPanel>
                        </Button>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>