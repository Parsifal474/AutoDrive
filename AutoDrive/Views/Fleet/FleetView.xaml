<UserControl x:Class="AutoDrive.Views.Fleet.FleetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <TabControl>
        <!-- Вкладка "Транспортные средства" -->
        <TabItem Header="Транспортные средства">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Управление автопарком" FontSize="22" FontWeight="Bold" Margin="0,0,0,10"/>

                <WrapPanel Grid.Row="1" Margin="0,0,0,10">
                    <TextBox Text="{Binding VehicleSearch, UpdateSourceTrigger=PropertyChanged}" Width="250" Margin="0,0,10,0" Tag="Поиск по марке, модели, госномеру"/>
                    <Button Content="Добавить" Command="{Binding AddVehicleCommand}" Margin="0,0,5,0" Padding="10,5"/>
                    <Button Content="Изменить" Command="{Binding EditVehicleCommand}" Margin="0,0,5,0" Padding="10,5"/>
                    <Button Content="Удалить" Command="{Binding DeleteVehicleCommand}" Padding="10,5"/>
                </WrapPanel>

                <DataGrid Grid.Row="2" ItemsSource="{Binding Vehicles}" SelectedItem="{Binding SelectedVehicle}"
                          AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                        <DataGridTextColumn Header="Марка" Binding="{Binding Brand}" Width="100"/>
                        <DataGridTextColumn Header="Модель" Binding="{Binding Model}" Width="100"/>
                        <DataGridTextColumn Header="Госномер" Binding="{Binding LicensePlate}" Width="100"/>
                        <DataGridTextColumn Header="Год" Binding="{Binding Year}" Width="60"/>
                        <DataGridTextColumn Header="ОСАГО до" Binding="{Binding InsuranceExpiry, StringFormat={}{0:dd.MM.yyyy}}" Width="100"/>
                        <DataGridTextColumn Header="Диагностика до" Binding="{Binding InspectionExpiry, StringFormat={}{0:dd.MM.yyyy}}" Width="100"/>
                        <DataGridTextColumn Header="ТО до" Binding="{Binding MaintenanceDue, StringFormat={}{0:dd.MM.yyyy}}" Width="100"/>
                        <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="80"/>
                        <DataGridTextColumn Header="ID инструктора" Binding="{Binding InstructorId}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </TabItem>

        <!-- Вкладка "Инструкторы" -->
        <TabItem Header="Инструкторы">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Управление инструкторами" FontSize="22" FontWeight="Bold" Margin="0,0,0,10"/>

                <WrapPanel Grid.Row="1" Margin="0,0,0,10">
                    <TextBox Text="{Binding InstructorSearch, UpdateSourceTrigger=PropertyChanged}" Width="250" Margin="0,0,10,0" Tag="Поиск по фамилии, имени, телефону"/>
                    <Button Content="Добавить" Command="{Binding AddInstructorCommand}" Margin="0,0,5,0" Padding="10,5"/>
                    <Button Content="Изменить" Command="{Binding EditInstructorCommand}" Margin="0,0,5,0" Padding="10,5"/>
                    <Button Content="Удалить" Command="{Binding DeleteInstructorCommand}" Padding="10,5"/>
                </WrapPanel>

                <DataGrid Grid.Row="2" ItemsSource="{Binding Instructors}" SelectedItem="{Binding SelectedInstructor}"
                          AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                        <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="100"/>
                        <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="100"/>
                        <DataGridTextColumn Header="Отчество" Binding="{Binding MiddleName}" Width="100"/>
                        <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="120"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="150"/>
                        <DataGridTextColumn Header="Ставка (руб/час)" Binding="{Binding HourlyRate}" Width="100"/>
                        <DataGridCheckBoxColumn Header="Активен" Binding="{Binding IsActive}" Width="70"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </TabItem>
    </TabControl>
</UserControl>