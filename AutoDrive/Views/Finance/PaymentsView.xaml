<UserControl x:Class="AutoDrive.Views.Finance.PaymentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Управление платежами" FontSize="22" FontWeight="Bold" Margin="0,0,0,10"/>

        <WrapPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="250" Margin="0,0,10,0" Tag="Поиск..."/>
            <Button Content="Добавить" Command="{Binding AddCommand}" Margin="0,0,5,0" Padding="10,5"/>
            <Button Content="Изменить" Command="{Binding EditCommand}" Margin="0,0,5,0" Padding="10,5"/>
            <Button Content="Удалить" Command="{Binding DeleteCommand}" Padding="10,5"/>
        </WrapPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Payments}" SelectedItem="{Binding SelectedPayment}"
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="ID курсанта" Binding="{Binding StudentId}" Width="80"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding Amount, StringFormat={}{0:N2} руб.}" Width="120"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding PaymentDate, StringFormat='dd.MM.yyyy'}" Width="100"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding PaymentType}" Width="100"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*"/>
                <DataGridCheckBoxColumn Header="Рассрочка" Binding="{Binding IsInstallment}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>