<UserControl x:Class="AutoDrive.Views.Finance.PaymentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:AutoDrive.ViewModels"
             mc:Ignorable="d">
    <UserControl.Resources>
        <DataTemplate x:Key="PaymentsTemplate">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Управление платежами" FontSize="22" FontWeight="Bold" Margin="0,0,0,10"/>

                <WrapPanel Grid.Row="1" Margin="0,0,0,10">
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="250" Margin="0,0,10,0" Tag="Поиск..."/>
                    <Button Content="Добавить" Command="{Binding AddCommand}" Margin="0,0,5,0" Padding="10,5"/>
                    <Button Content="Изменить" Command="{Binding EditCommand}" Margin="0,0,5,0" Padding="10,5"/>
                    <Button Content="Удалить" Command="{Binding DeleteCommand}" Padding="10,5"/>
                </WrapPanel>

                <DataGrid Grid.Row="2" ItemsSource="{Binding Payments}" SelectedItem="{Binding SelectedPayment}"
                          AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                        <DataGridTextColumn Header="ID курсанта" Binding="{Binding StudentId}" Width="80"/>
                        <DataGridTextColumn Header="Сумма" Binding="{Binding Amount, StringFormat={}{0:N2} руб.}" Width="120"/>
                        <DataGridTextColumn Header="Дата" Binding="{Binding PaymentDate, StringFormat={}{0:dd.MM.yyyy}}" Width="100"/>
                        <DataGridTextColumn Header="Тип" Binding="{Binding PaymentType}" Width="100"/>
                        <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*"/>
                        <DataGridCheckBoxColumn Header="Рассрочка" Binding="{Binding IsInstallment}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="ReportsTemplate">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Финансовые отчёты" FontSize="22" FontWeight="Bold" Margin="0,0,0,10"/>

                <WrapPanel Grid.Row="1" Margin="0,0,0,10">
                    <TextBlock Text="Период:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <DatePicker SelectedDate="{Binding StartDate}" Width="120" Margin="0,0,10,0"/>
                    <TextBlock Text="—" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <DatePicker SelectedDate="{Binding EndDate}" Width="120" Margin="0,0,20,0"/>
                    <Button Content="Выручка" Command="{Binding CalculateRevenueCommand}" Padding="10,5" Margin="0,0,5,0"/>
                    <Button Content="Задолженности" Command="{Binding CalculateDebtsCommand}" Padding="10,5" Margin="0,0,5,0"/>
                    <Button Content="Экспорт в Excel" Command="{Binding ExportToExcelCommand}" Padding="10,5"/>
                </WrapPanel>

                <Border Grid.Row="2" Background="LightGray" Padding="10" Margin="0,10">
                    <TextBlock Text="{Binding TotalRevenue, StringFormat=Общая выручка за период: {0:N2} руб.}" FontSize="16" FontWeight="Bold"/>
                </Border>

                <TextBlock Grid.Row="3" Text="Список задолженностей" FontSize="16" FontWeight="Bold" Margin="0,10,0,5"/>

                <DataGrid Grid.Row="4" ItemsSource="{Binding Debts}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Курсант" Binding="{Binding StudentName}" Width="200"/>
                        <DataGridTextColumn Header="Оплачено" Binding="{Binding PaidAmount, StringFormat={}{0:N2} руб.}" Width="120"/>
                        <DataGridTextColumn Header="Задолженность" Binding="{Binding DebtAmount, StringFormat={}{0:N2} руб.}" Width="120"/>
                        <DataGridCheckBoxColumn Header="Оплачен полностью" Binding="{Binding IsFullyPaid}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <TabControl>
        <TabItem Header="Платежи" ContentTemplate="{StaticResource PaymentsTemplate}" Content="{Binding}" />
        <TabItem Header="Отчёты" ContentTemplate="{StaticResource ReportsTemplate}" Content="{Binding ReportsViewModel}" />
    </TabControl>
</UserControl>